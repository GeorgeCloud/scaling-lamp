<div class="row">
  <div class="col-sm-6 col-sm-offset-3 mt-2">
    <!-- {{post}} -->
    <form action="/posts/{{this._id}}" method="POST">
      <!-- <input type="hidden" name="comment_id" value="{{this._id}}"> -->

      <div class="form-group">
        <textarea name="content" class="form-control" cols="20" rows="1"
          id="reply-content"></textarea>
      </div>

      <div class='text-right mt-2'>
        <button type="submit" class="btn btn-primary">Reply</button>
      </div>
    </form>
  </div>
</div>
